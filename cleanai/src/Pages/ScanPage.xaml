<Page
    x:Class="CleanAI.Pages.ScanPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="#121212">
    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Сканирование диска" FontSize="24" FontWeight="SemiBold"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
            <ComboBox x:Name="DriveCombo" Width="180" PlaceholderText="Выберите диск"/>
            <Button Content="Сканировать" Click="OnScan" Background="#6366f1" CornerRadius="4"/>
        </StackPanel>

        <Border Grid.Row="2" Background="#1e293b" CornerRadius="8" Padding="12">
            <StackPanel>
                <TextBlock x:Name="CurrentPathText" Text="Текущий путь: —"/>
                <TextBlock x:Name="FilesCountText" Text="Файлов: 0"/>
                <TextBlock x:Name="SizeText" Text="Размер: 0 Б"/>
                <ProgressBar x:Name="ScanProgressBar" IsIndeterminate="True" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
