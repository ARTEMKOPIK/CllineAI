name: Auto-recover failed build

on:
  workflow_run:
    workflows:
      - Build and release EXE
    types:
      - completed

permissions:
  actions: write
  contents: read
  issues: write

jobs:
  retry-failed-jobs:
    if: >-
      github.event.workflow_run.conclusion == 'failure' &&
      github.event.workflow_run.run_attempt == 1
    runs-on: ubuntu-latest
    steps:
      - name: Retry only failed jobs once
        uses: actions/github-script@v7
        with:
          script: |
            const runId = context.payload.workflow_run.id;
            await github.rest.actions.reRunWorkflowFailedJobs({
              owner: context.repo.owner,
              repo: context.repo.repo,
              run_id: runId,
            });

  create-incident-issue:
    if: >-
      github.event.workflow_run.conclusion == 'failure' &&
      github.event.workflow_run.run_attempt > 1
    runs-on: ubuntu-latest
    steps:
      - name: Create issue with failing run details
        uses: actions/github-script@v7
        with:
          script: |
            const run = context.payload.workflow_run;
            const title = `[CI Autofix] Build failed on ${run.head_branch} @ ${run.head_sha.substring(0,7)}`;

            const existing = await github.rest.search.issuesAndPullRequests({
              q: `repo:${context.repo.owner}/${context.repo.repo} is:issue is:open in:title "${title}"`
            });

            if (existing.data.total_count > 0) {
              core.info('Issue already exists, skipping creation.');
              return;
            }

            const body = [
              'Автоматический ретрай не помог: сборка всё ещё падает.',
              '',
              `- Workflow: **${run.name}**`,
              `- Branch: **${run.head_branch}**`,
              `- Commit: ${run.head_sha}`,
              `- Run: ${run.html_url}`,
              '',
              '### Что сделать',
              '- [ ] Воспроизвести ошибку локально',
              '- [ ] Исправить причину падения',
              '- [ ] Убедиться, что GitHub Actions снова зелёный',
            ].join('\n');

            try {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: ['ci', 'autofix-needed']
              });
            } catch (error) {
              core.warning(`Issue labels are missing, creating issue without labels: ${error.message}`);
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
              });
            }
